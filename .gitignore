target/
__pycache__/
*.o
*.so
!runtime/libmind_cuda_linux-x64.so
*.pyc
runtime/target/
*.log
.pytest_cache/
.ruff_cache/
# Keep generated results JSON in-tree (they *are* the deliverable), but ignore
# purely-local working outputs
/tmp/
# Local memory tool artifacts
AGENTS.md

# C5: parent paper PDF (deanonymised) — never commit
*.pdf
!docs/img/*.pdf
# H6: STARGA-private MIND toolchain artefacts (proprietary; not for public release)
Mind.toml
runtime/
target/
*.mind
# Exception: arch-mind L1 governance rules profile is a Q16.16 config
# file (no proprietary toolchain leakage); MUST be tracked so CI can
# verify rules. Safe to ship — `.arch-mind/rules.mind` is config not code.
!.arch-mind/rules.mind
# M3: __pycache__ everywhere
**/__pycache__/
